#include "main.h"
#include <stdio.h>
/**
* _isupper - prints whether letter is uppercase
* @c: is the int in use
* Return: 1 if c is uppercase
* Return 0 otherwise
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{}
return (0);
}
